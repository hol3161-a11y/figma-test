# SANGREEN 호텔 웹사이트

산그린호텔의 공식 웹사이트입니다. "The Day of Rest" 콘셉트로 지친 일상 속 자연을 느낄 수 있는 호텔 경험을 제공합니다.

## 🚀 빠른 시작

### 필수 요구사항

- Node.js 18.0 이상
- pnpm (권장) 또는 npm

### 설치 방법

```bash
# 의존성 패키지 설치
pnpm install
# 또는
npm install
```

### 개발 서버 실행

```bash
# 개발 모드로 실행
pnpm dev
# 또는
npm run dev
```

개발 서버는 기본적으로 `http://localhost:5173`에서 실행됩니다.

## 📦 배포하기

### 1. 프로덕션 빌드

```bash
pnpm build
# 또는
npm run build
```

빌드가 완료되면 `dist` 폴더에 최적화된 정적 파일들이 생성됩니다.

### 2. 배포 방법

#### Vercel로 배포

1. [Vercel](https://vercel.com)에 회원가입/로그인
2. "New Project" 클릭
3. GitHub 저장소 연결
4. Framework Preset: Vite 선택
5. Deploy 클릭

#### Netlify로 배포

1. [Netlify](https://netlify.com)에 회원가입/로그인
2. "Add new site" > "Import an existing project"
3. GitHub 저장소 연결
4. Build settings:
   - Build command: `npm run build`
   - Publish directory: `dist`
5. Deploy 클릭

#### GitHub Pages로 배포

1. `vite.config.ts` 파일에 base 경로 추가:
   ```typescript
   export default defineConfig({
     base: '/저장소이름/',
     // ... 나머지 설정
   })
   ```

2. 빌드 후 배포:
   ```bash
   pnpm build
   cd dist
   git init
   git add -A
   git commit -m 'deploy'
   git push -f git@github.com:사용자명/저장소이름.git main:gh-pages
   ```

#### 일반 웹 호스팅 (FTP)

1. `pnpm build` 실행
2. `dist` 폴더의 모든 파일을 FTP로 웹 서버에 업로드
3. 웹 서버가 SPA(Single Page Application)를 지원하도록 설정
   - 모든 경로를 `index.html`로 리다이렉트하도록 설정 필요

### 3. 빌드 미리보기

빌드된 결과물을 로컬에서 미리 확인하려면:

```bash
pnpm preview
# 또는
npm run preview
```

## 🏗️ 프로젝트 구조

```
.
├── src/
│   ├── app/
│   │   ├── App.tsx              # 메인 애플리케이션 컴포넌트
│   │   └── components/          # 재사용 가능한 컴포넌트들
│   ├── imports/
│   │   ├── 0메인디자인.tsx      # Figma에서 가져온 메인 디자인
│   │   └── svg-odl0poeqkm.ts    # SVG 경로 데이터
│   ├── styles/
│   │   ├── index.css            # 스타일 진입점
│   │   ├── fonts.css            # 폰트 설정
│   │   ├── tailwind.css         # Tailwind 설정
│   │   └── theme.css            # 테마 변수
│   └── main.tsx                 # 애플리케이션 진입점
├── index.html                   # HTML 템플릿
├── package.json                 # 프로젝트 의존성
└── vite.config.ts              # Vite 설정
```

## 🎨 주요 기능

- **반응형 디자인**: 모바일, 태블릿, 데스크톱 모든 화면 크기 지원
- **객실 정보**: 101호부터 104호까지의 객실 정보 제공
- **부대시설**: 수영장, 피트니스, 라운지, 루프탑 가든 소개
- **관광 정보**: 남이섬 등 주변 관광지 안내
- **예약 시스템**: 체크인/체크아웃 날짜 선택 인터페이스

## 🛠️ 기술 스택

- **Frontend Framework**: React 18.3
- **Build Tool**: Vite 6.3
- **Styling**: Tailwind CSS 4.1
- **Language**: TypeScript
- **UI Components**: Radix UI
- **Icons**: Lucide React
- **Fonts**: 
  - Noto Sans KR (한글)
  - Nanum Gothic (한글)
  - Inter (영문)
  - Baloo Bhai 2 (영문)

## 📝 환경 변수

현재 이 프로젝트는 환경 변수가 필요하지 않습니다. 모든 설정은 코드 내에 포함되어 있습니다.

만약 API 키나 기타 설정이 필요한 경우, `.env` 파일을 생성하고 다음과 같이 추가하세요:

```env
VITE_API_URL=your_api_url_here
```

## 🌐 브라우저 지원

- Chrome (최신 2개 버전)
- Firefox (최신 2개 버전)
- Safari (최신 2개 버전)
- Edge (최신 2개 버전)

## 📄 라이선스

Copyright ⓒ 산그린호텔 All rights reserved.

## 📞 문의

- **대표자**: 이엽
- **이메일**: sidezzz@naver.com
- **전화**: 031-584-5748
- **휴대폰**: 010-2206-5748
- **주소**: 서울 종로구 율곡로 179 메이플레이스, 산그린호텔
